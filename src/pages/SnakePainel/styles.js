import styled from 'styled-components';

export const Container = styled.div`
  border: 2px solid var(--border-painel);
  width: 60vw;
  height: 70vh;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
`;

export const Squad = styled.div`
  background: var(--squad-1);
  width: 3vw;
  height: 5vh;
`;
